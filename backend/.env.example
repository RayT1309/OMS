SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

DATABASE_URL=

# Optional: set this and configure the same value as a Vercel env var to
# authenticate the /api/cron/generate-reports endpoint (see vercel.json).
CRON_SECRET=
